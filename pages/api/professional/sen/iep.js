import { getAdminAuth, getAdminFirestore } from '../../../../src/security/firebaseAdmin.js';
import { normalizeCanonicalStudent } from '../../../../src/platform/canonicalStudentContract.js';
import { normalizeServiceAssignment } from '../../../../src/platform/serviceAssignmentContract.js';
import { getActiveRelationship } from '../../../../src/security/relationshipStore.js';

function bearerToken(req) { const header = req.headers.authorization || req.headers.Authorization; if (typeof header !== 'string') return null; const match = header.match(/^Bearer\s+(.+)$/i); return match ? match[1] : null; }
function safeString(value, max = 5000) { return String(value || '').trim().slice(0, max); }
function isSenProfessionalRole(role) { return ['educator', 'sen_educator'].includes(String(role || '').toLowerCase()); }
async function authenticate(req, res) { const token = bearerToken(req); if (!token) { res.status(401).json({ error: 'Authentication required.' }); return null; } let decoded; try { decoded = await getAdminAuth().verifyIdToken(token); } catch (_) { res.status(401).json({ error: 'Invalid or expired authentication token.' }); return null; } if (!isSenProfessionalRole(decoded.role)) { res.status(403).json({ error: 'SEN professional access required.' }); return null; } return decoded; }
async function resolveStudent(db, studentId) { const candidates = [db.collection('students').doc(studentId), db.collection('users').doc(studentId)]; for (const ref of candidates) { const snap = await ref.get(); if (snap.exists) return { ref, data: snap.data() || {} }; } return null; }
async function isAssignedToSen(db, studentId, student, professionalId) { const canonical = await getActiveRelationship({ db, subjectPersonId: studentId, relatedPersonId: professionalId, type: 'sen_professional', domain: 'sen' }); if (canonical) return { allowed: true, source: 'canonical', relationship: canonical }; const canonicalSnapshot = await db.collection('relationships').where('subjectPersonId', '==', studentId).where('relatedPersonId', '==', professionalId).where('type', '==', 'sen_professional').limit(20).get(); if (!canonicalSnapshot.empty) return { allowed: false, source: 'canonical', relationship: null }; const raw = student?.relationships?.assignments?.sen; const assignment = normalizeServiceAssignment(typeof raw === 'string' ? { primaryProfessionalId: raw } : (raw || {}), 'sen'); if (assignment.primaryProfessionalId === professionalId || assignment.backupProfessionalId === professionalId || assignment.team.some((member) => member.professionalId === professionalId && member.status === 'active')) return { allowed: true, source: 'legacy-assignment', relationship: null }; const legacy = student?.assignedStaff || {}; if (legacy.senId === professionalId || legacy.educatorId === professionalId || student?.assignedSENEducatorId === professionalId) return { allowed: true, source: 'legacy', relationship: null }; return { allowed: false, source: 'none', relationship: null }; }
export default async function handler(req, res) { if (!['GET', 'POST'].includes(req.method)) { res.setHeader('Allow', 'GET, POST'); return res.status(405).json({ error: 'Method not allowed.' }); } const decoded = await authenticate(req, res); if (!decoded) return; const studentId = String(req.query?.studentId || '').trim(); if (!studentId) return res.status(400).json({ error: 'studentId is required.' }); try { const db = getAdminFirestore(); const resolved = await resolveStudent(db, studentId); if (!resolved) return res.status(404).json({ error: 'Student record not found.' }); const student = resolved.data; const access = await isAssignedToSen(db, studentId, student, decoded.uid); if (!access.allowed) return res.status(403).json({ error: 'This student is not assigned to your SEN professional account.' }); const canonicalStudent = normalizeCanonicalStudent(student, studentId); const responseBase = { student: { authUid: canonicalStudent.authUid, ssStudentId: canonicalStudent.ssStudentId, name: canonicalStudent.identity.preferredName || canonicalStudent.identity.legalName || 'Unknown Student', schoolName: canonicalStudent.institutionMemberships.find((m) => m.status === 'active')?.institutionName || 'N/A', grade: canonicalStudent.academic.current.grade || 'N/A', section: canonicalStudent.academic.current.section || '' } }; if (req.method === 'GET') { const snapshot = await db.collection('sen').doc(studentId).collection('iep_records').orderBy('createdAt', 'desc').limit(20).get(); return res.status(200).json({ ...responseBase, ieps: snapshot.docs.map((doc) => ({ id: doc.id, ...doc.data() })) }); } const body = req.body || {}; const plop = safeString(body.plop, 10000); const goals = Array.isArray(body.goals) ? body.goals.map((goal) => safeString(goal, 1000)).filter(Boolean).slice(0, 30) : []; const accommodations = Array.isArray(body.accommodations) ? body.accommodations.map((item) => safeString(item, 300)).filter(Boolean).slice(0, 30) : []; if (!plop && !goals.length && !accommodations.length) return res.status(400).json({ error: 'Cannot save an empty IEP.' }); const relationshipId = access.relationship?.relationshipId || access.relationship?.id || null; if (!relationshipId) return res.status(403).json({ error: 'A canonical SEN professional relationship is required for new IEP records.' }); const collection = db.collection('sen').doc(studentId).collection('iep_records'); const recordRef = collection.doc(); const now = new Date().toISOString(); let record; await db.runTransaction(async (transaction) => { const latestSnapshot = await transaction.get(collection.orderBy('version', 'desc').limit(1)); const activeSnapshot = await transaction.get(collection.where('status', '==', 'active')); const latestVersion = latestSnapshot.empty ? 0 : Number(latestSnapshot.docs[0].data()?.version || 0); const nextVersion = Number.isFinite(latestVersion) ? latestVersion + 1 : 1; record = { iepId: recordRef.id, authUid: canonicalStudent.authUid, ssStudentId: canonicalStudent.ssStudentId, providerId: decoded.uid, providerRole: decoded.role, relationshipId, service: 'sen', status: 'active', version: nextVersion, plop, goals, accommodations, createdAt: now, updatedAt: now }; for (const doc of activeSnapshot.docs) transaction.update(doc.ref, { status: 'superseded', supersededBy: recordRef.id, supersededAt: now, updatedAt: now }); transaction.set(recordRef, record); }); try { await db.collection('auditEvents').add({ kind: 'sen_iep_created', actorUid: decoded.uid, actorRole: decoded.role, studentId, iepId: record.iepId, version: record.version, relationshipId, timestamp: now }); } catch (auditError) { console.error('[professional/sen/iep] audit write failed:', auditError?.message || auditError); } return res.status(201).json({ saved: true, record }); } catch (error) { console.error('[professional/sen/iep] failed:', error?.message || error); return res.status(500).json({ error: 'Unable to complete the SEN IEP request.' }); } }
