# Vercel production deployment trigger

The onboarding popup fix is already merged into `main`. This file exists only to trigger the Git-integrated production deployment so Vercel rebuilds the current `main` commit.
