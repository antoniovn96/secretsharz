import React, { useMemo, useState } from 'react';
import { Search, X, Download, SlidersHorizontal, ChevronUp, ChevronDown, Eye, Pencil, Trash2 } from 'lucide-react';
import { getProfileIdentity } from '../../platform/profileIdentity';

const ParentDirectoryTable = ({ users = [], isLoading = false, onViewDetails, onEdit, onDelete, theme = 'light' }) => {
  const [searchQuery, setSearchQuery] = useState('');
  const [filterStatus, setFilterStatus] = useState('all');
  const [showFilters, setShowFilters] = useState(false);
  const [sortConfig, setSortConfig] = useState({ key: 'name', direction: 'asc' });
  const dark = theme === 'dark';
  const surface = dark ? 'bg-[#151515] border-[#292929]' : 'bg-white border-slate-100';
  const subtle = dark ? 'bg-[#111111] border-[#292929]' : 'bg-slate-50 border-slate-100';
  const input = dark ? 'bg-[#111111] border-[#333] text-white placeholder:text-[#777]' : 'bg-slate-50 border-slate-200 text-slate-700 placeholder:text-slate-400';
  const primaryText = dark ? 'text-white' : 'text-slate-900';
  const secondaryText = dark ? 'text-[#b5b5b5]' : 'text-slate-600';
  const mutedText = dark ? 'text-[#777]' : 'text-slate-400';
  const border = dark ? 'border-[#292929]' : 'border-slate-100';
  const hover = dark ? 'hover:bg-white/[0.04]' : 'hover:bg-slate-50';

  const filteredUsers = useMemo(() => {
    let result = [...users];
    const q = searchQuery.trim().toLowerCase();
    if (q) result = result.filter(parent => { const identity = getProfileIdentity(null, parent); return [identity.name, identity.email, parent.phone, parent.id, parent.city, parent.location, parent.occupation].some(value => String(value || '').toLowerCase().includes(q)); });
    if (filterStatus !== 'all') result = result.filter(parent => (parent.status || 'active') === filterStatus);
    result.sort((a,b) => { const av=String(sortConfig.key==='name'?getProfileIdentity(null,a).name:sortConfig.key==='children'?(a.childrenCount||0):(a.status||'active')).toLowerCase(); const bv=String(sortConfig.key==='name'?getProfileIdentity(null,b).name:sortConfig.key==='children'?(b.childrenCount||0):(b.status||'active')).toLowerCase(); if(av===bv)return 0; const order=av<bv?-1:1; return sortConfig.direction==='asc'?order:-order; });
    return result;
  }, [users, searchQuery, filterStatus, sortConfig]);
  const handleSort = key => setSortConfig(prev => ({ key, direction: prev.key === key && prev.direction === 'asc' ? 'desc' : 'asc' }));
  const exportCsv = () => { const headers=['Parent ID','Name','Email','Phone','Children','Status']; const rows=filteredUsers.map(parent=>{const identity=getProfileIdentity(null,parent);return[parent.id,identity.name,identity.email,parent.phone||'',parent.childrenCount||0,parent.status||'active'];}); const escape=value=>`"${String(value).replace(/"/g,'""')}"`; const csv=[headers,...rows].map(row=>row.map(escape).join(',')).join('\n'); const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'}); const url=URL.createObjectURL(blob); const anchor=document.createElement('a'); anchor.href=url; anchor.download=`secret-sharz-parents-${new Date().toISOString().slice(0,10)}.csv`; anchor.click(); URL.revokeObjectURL(url); };
  if (isLoading) return <div className={`${surface} rounded-xl shadow-sm overflow-hidden`}><div className="p-8 flex flex-col items-center justify-center h-64"><div className={`w-10 h-10 border-4 rounded-full animate-spin mb-4 ${dark?'border-[#333] border-t-white':'border-slate-200 border-t-black'}`}/><p className={`${secondaryText} font-medium`}>Loading parents...</p></div></div>;
  return <div className={`${surface} rounded-xl shadow-sm overflow-hidden`}><div className={`p-4 border-b ${border} space-y-4`}><div className="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-3"><div className="flex items-center gap-3 w-full lg:w-auto"><div className="relative flex-1 lg:w-[380px]"><Search className={`w-4 h-4 ${mutedText} absolute left-3 top-1/2 -translate-y-1/2`}/><input value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} placeholder="Search parent name, email, phone..." className={`w-full pl-10 pr-9 py-2.5 border rounded-lg text-sm focus:outline-none focus:ring-1 ${input} ${dark?'focus:ring-white':'focus:ring-black'}`}/>{searchQuery&&<button onClick={()=>setSearchQuery('')} className={`absolute right-3 top-1/2 -translate-y-1/2 ${mutedText}`}><X className="w-4 h-4"/></button>}</div><button onClick={()=>setShowFilters(prev=>!prev)} className={`flex items-center gap-2 px-3 py-2.5 rounded-lg border text-sm font-semibold ${showFilters? (dark?'border-white bg-white text-black':'border-black bg-black text-white') : `${dark?'border-[#333] text-[#aaa]':'border-slate-200 text-slate-600'} ${hover}`}`}><SlidersHorizontal className="w-4 h-4"/> Filters</button></div><div className="flex items-center gap-2"><span className={`text-sm font-medium ${secondaryText}`}>{filteredUsers.length} of {users.length} parents</span><button onClick={exportCsv} className={`flex items-center gap-2 px-3 py-2.5 border rounded-lg text-sm font-semibold ${dark?'border-[#333] text-[#bbb] hover:bg-white/[0.04]':'border-slate-200 text-slate-600 hover:bg-slate-50'}`}><Download className="w-4 h-4"/> Export CSV</button></div></div>{showFilters&&<div className={`p-3 rounded-lg border ${subtle}`}><label className={`text-xs font-bold ${mutedText}`}>STATUS<select value={filterStatus} onChange={e=>setFilterStatus(e.target.value)} className={`mt-1 w-full md:w-64 px-3 py-2 border rounded-lg text-sm ${input}`}><option value="all">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select></label></div>}</div><div className="overflow-x-auto"><table className="w-full min-w-[900px]"><thead><tr className={`${subtle} border-b ${border}`}><Header label="Parent" column="name" onSort={handleSort} config={sortConfig} dark={dark}/><th className={`px-4 py-3 text-left text-xs font-bold uppercase tracking-wider ${mutedText}`}>Contact</th><Header label="Linked Children" column="children" onSort={handleSort} config={sortConfig} dark={dark}/><th className={`px-4 py-3 text-left text-xs font-bold uppercase tracking-wider ${mutedText}`}>Location</th><Header label="Status" column="status" onSort={handleSort} config={sortConfig} dark={dark}/><th className={`px-4 py-3 text-right text-xs font-bold uppercase tracking-wider ${mutedText}`}>Actions</th></tr></thead><tbody className={`divide-y ${dark?'divide-[#252525]':'divide-slate-100'}`}>{filteredUsers.length===0?<tr><td colSpan={6} className="px-4 py-14 text-center"><p className={`${secondaryText} font-semibold`}>No parents found</p><p className={`text-sm ${mutedText} mt-1`}>Try adjusting your search or filters.</p></td></tr>:filteredUsers.map(parent=>{const identity=getProfileIdentity(null,parent);const status=parent.status||'active';return <tr key={parent.id} className={`${hover} transition-colors cursor-pointer`} onClick={()=>onViewDetails?.(parent)}><td className="px-4 py-3"><div className="flex items-center gap-3">{identity.photoURL?<img src={identity.photoURL} alt="" className={`w-10 h-10 rounded-lg object-cover shadow-sm ${dark?'bg-[#222]':'bg-slate-100'}`}/>:<div className={`w-10 h-10 rounded-lg flex items-center justify-center font-bold text-sm shadow-sm ${dark?'bg-white text-black':'bg-black text-white'}`}>{identity.initial}</div>}<div className="min-w-0"><p className={`font-semibold truncate max-w-[220px] ${primaryText}`}>{identity.name}</p><p className={`text-xs font-mono truncate max-w-[220px] ${mutedText}`}>{parent.id}</p></div></div></td><td className="px-4 py-3"><p className={`text-sm font-semibold ${secondaryText}`}>{identity.email||'No email'}</p><p className={`text-xs mt-0.5 ${mutedText}`}>{parent.phone||'No phone'}</p></td><td className="px-4 py-3"><span className={`inline-flex px-2.5 py-1 rounded-lg text-xs font-bold ${dark?'bg-white text-black':'bg-slate-100 text-slate-700'}`}>{parent.childrenCount||0} {parent.childrenCount===1?'child':'children'}</span></td><td className="px-4 py-3"><span className={`text-sm ${secondaryText}`}>{parent.location||parent.city||'Not specified'}</span></td><td className="px-4 py-3"><span className={`inline-flex px-2.5 py-1 rounded-lg text-xs font-bold ${status==='active'?(dark?'bg-white text-black':'bg-slate-100 text-slate-800'):(dark?'bg-[#222] text-[#999]':'bg-slate-100 text-slate-600')}`}>{status==='active'?'Active':'Inactive'}</span></td><td className="px-4 py-3"><div className="flex items-center justify-end gap-1" onClick={e=>e.stopPropagation()}><button title="View details" onClick={()=>onViewDetails?.(parent)} className={`p-2 rounded-lg ${dark?'text-[#888] hover:text-white hover:bg-white/[0.06]':'text-slate-400 hover:text-black hover:bg-slate-100'}`}><Eye className="w-4 h-4"/></button>{onEdit&&<button title="Edit parent" onClick={()=>onEdit(parent)} className={`p-2 rounded-lg ${dark?'text-[#888] hover:text-white hover:bg-white/[0.06]':'text-slate-400 hover:text-black hover:bg-slate-100'}`}><Pencil className="w-4 h-4"/></button>}{onDelete&&<button title="Delete parent" onClick={()=>onDelete(parent)} className={`p-2 rounded-lg ${dark?'text-[#888] hover:text-white hover:bg-white/[0.06]':'text-slate-400 hover:text-black hover:bg-slate-100'}`}><Trash2 className="w-4 h-4"/></button>}</div></td></tr>})}</tbody></table></div></div>;
};
const Header=({label,column,onSort,config,dark})=><th className="px-4 py-3 text-left"><button onClick={()=>onSort(column)} className={`flex items-center gap-1 text-xs font-bold uppercase tracking-wider ${dark?'text-[#888] hover:text-white':'text-slate-500 hover:text-slate-800'}`}>{label}{config.key===column?(config.direction==='asc'?<ChevronUp className="w-4 h-4"/>:<ChevronDown className="w-4 h-4"/>):<ChevronDown className="w-4 h-4 opacity-40"/>}</button></th>;
export default ParentDirectoryTable;
