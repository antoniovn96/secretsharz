import { getStudentPath } from './studentRecordModel.js';

const SERVICE_KEYS = ['career', 'wellbeing', 'sen'];
const EDUCATION_TIERS = ['tenth', 'twelfth', 'graduate', 'postGraduate'];

const firstDefined = (...values) => values.find((value) => value !== undefined && value !== null && value !== '');
const asArray = (value) => Array.isArray(value) ? value : value ? [value] : [];
const asStringArray = (value) => asArray(value).map(String).filter(Boolean);

function normalisePath(data = {}) {
  const raw = String(firstDefined(data.primary_path, data.studentTrack, data.path, data.service, '')).trim().toLowerCase();
  if (raw === 'psychology' || raw === 'psychologist' || raw === 'counselling' || raw === 'counseling') return 'wellbeing';
  if (raw === 'special_education' || raw === 'special education' || raw === 'educator') return 'sen';
  if (raw === 'career_guidance' || raw === 'career guidance') return 'career';
  return SERVICE_KEYS.includes(raw) ? raw : getStudentPath(data);
}

function normaliseGuardian(data = {}) {
  const guardians = [];
  const existing = asArray(data.family?.guardians || data.guardians || data.parents);
  existing.forEach((guardian) => {
    if (!guardian || typeof guardian !== 'object') return;
    guardians.push({ accountId: firstDefined(guardian.accountId, guardian.uid, guardian.id, null), relationship: firstDefined(guardian.relationship, guardian.parentType, guardian.relation, 'guardian'), name: firstDefined(guardian.name, guardian.fullName, ''), email: firstDefined(guardian.email, ''), phone: firstDefined(guardian.phone, guardian.contactNumber, guardian.parentContact, ''), countryCode: firstDefined(guardian.countryCode, guardian.contactCountryCode, null), legalGuardian: Boolean(guardian.legalGuardian), invitationStatus: firstDefined(guardian.invitationStatus, guardian.accountStatus, null), consentStatus: firstDefined(guardian.consentStatus, null) });
  });
  const guardianRelationships = data.guardianRelationships && typeof data.guardianRelationships === 'object' ? data.guardianRelationships : {};
  Object.entries(guardianRelationships).forEach(([accountId, relationship]) => { if (!guardians.some((guardian) => guardian.accountId === accountId)) guardians.push({ accountId, relationship: firstDefined(relationship, 'guardian'), name: '', email: '', phone: '', countryCode: null, legalGuardian: false, invitationStatus: null, consentStatus: null }); });
  const legacyAccountId = firstDefined(data.parentUid, data.parentId, data.parent?.uid, null); const legacyName = firstDefined(data.parentName, data.parent?.name, ''); const legacyPhone = firstDefined(data.parentContact, data.parent?.phone, ''); const legacyEmail = firstDefined(data.parentEmail, data.parent?.email, '');
  if (legacyAccountId || legacyName || legacyPhone || legacyEmail) { if (!guardians.some((guardian) => guardian.accountId === legacyAccountId && guardian.name === legacyName)) guardians.push({ accountId: legacyAccountId, relationship: firstDefined(data.parentRelationship, 'guardian'), name: legacyName, email: legacyEmail, phone: legacyPhone, countryCode: firstDefined(data.contactCountryCode, null), legalGuardian: false, invitationStatus: null, consentStatus: null }); }
  [{ relationship: 'father', name: data.fatherName, phone: data.fatherPhone, email: data.fatherEmail }, { relationship: 'mother', name: data.motherName, phone: data.motherPhone, email: data.motherEmail }].forEach((parent) => { if (!parent.name && !parent.phone && !parent.email) return; if (!guardians.some((guardian) => String(guardian.relationship).toLowerCase() === parent.relationship && guardian.name === parent.name)) guardians.push({ accountId: null, relationship: parent.relationship, name: firstDefined(parent.name, ''), email: firstDefined(parent.email, ''), phone: firstDefined(parent.phone, ''), countryCode: firstDefined(data.contactCountryCode, null), legalGuardian: false, invitationStatus: null, consentStatus: null }); });
  return guardians;
}

function normaliseAssignments(data = {}) {
  const staff = data.assignedStaff || {};
  const canonical = data.relationships?.assignments || {};
  return { career: firstDefined(canonical.career, staff.careerId, data.assignedCareerCounsellorId, data.assignedCareerCoachId, null), wellbeing: firstDefined(canonical.wellbeing, staff.psychologistId, staff.psychologyId, data.assignedPsychologistId, data.assignedCounsellorId, null), sen: firstDefined(canonical.sen, staff.senId, staff.educatorId, data.assignedSENEducatorId, null) };
}

function normaliseServices(data = {}) {
  const services = {};
  const existing = data.services || {};
  const hasCanonicalServices = SERVICE_KEYS.some((service) => existing[service] && typeof existing[service] === 'object');
  const activePath = normalisePath(data);
  SERVICE_KEYS.forEach((service) => {
    const current = existing[service];
    const explicit = current && typeof current === 'object' ? current : {};
    const active = hasCanonicalServices ? explicit.status === 'active' || explicit.active === true : service === activePath;
    const status = hasCanonicalServices ? (explicit.status === 'active' || explicit.active === true ? 'active' : firstDefined(explicit.status, 'inactive')) : (active ? 'active' : 'inactive');
    services[service] = { status, enrolledAt: firstDefined(explicit.enrolledAt, data.serviceEnrollment?.[service]?.enrolledAt, null), source: firstDefined(explicit.source, data.serviceEnrollment?.[service]?.source, null), consentStatus: firstDefined(explicit.consentStatus, data.serviceEnrollment?.[service]?.consentStatus, null) };
  });
  return services;
}

function normaliseEducationTier(tier = {}, legacy = {}) { const source = { ...legacy, ...tier }; return { schoolName: firstDefined(source.schoolName, source.institutionName, ''), marksType: firstDefined(source.marksType, 'percentage'), marksValue: firstDefined(source.marksValue, source.percentage, source.cgpa, ''), marksMax: firstDefined(source.marksMax, ''), marksObtained: firstDefined(source.marksObtained, ''), subjects: asStringArray(source.subjects) }; }
function normaliseAcademic(data = {}) { const existing = data.academic || {}; const current = existing.current || {}; const history = existing.history || {}; const legacyEducation = data.education || {}; const legacyTiers = { tenth: legacyEducation.tenth || {}, twelfth: legacyEducation.twelfth || {}, graduate: legacyEducation.graduate || {}, postGraduate: legacyEducation.postGraduate || {} }; const currentSubjects = firstDefined(current.subjects, existing.subjects, data.subjects, []); const currentInstitutionId = firstDefined(current.institutionId, existing.institutionId, data.institution?.id, data.institutionId, data.institutionID, ''); const currentInstitutionName = firstDefined(current.institutionName, existing.institutionName, data.institution?.name, data.institutionName, data.schoolName, legacyEducation.schoolName, ''); const currentAcademicYear = firstDefined(current.academicYear, existing.academicYear, data.institution?.academicYear, data.academicYear, ''); const academic = { current: { institutionId: currentInstitutionId, institutionName: currentInstitutionName, academicYear: currentAcademicYear, grade: firstDefined(current.grade, existing.grade, data.grade, data.gradeOrCourse, ''), section: firstDefined(current.section, existing.section, data.section, ''), curriculum: firstDefined(current.curriculum, existing.curriculum, data.curriculum, data.board, ''), stream: firstDefined(current.stream, existing.stream, data.stream1112, data.stream, ''), subjects: asStringArray(currentSubjects) }, history: {}, highestLevel: firstDefined(existing.highestLevel, legacyEducation.highestLevel, data.highestEducationLevel, ''), address: firstDefined(existing.address, legacyEducation.address, data.address, ''), yearOfPassing: firstDefined(existing.yearOfPassing, legacyEducation.yearOfPassing, data.yearOfPassing, ''), isPursuing: typeof existing.isPursuing === 'boolean' ? existing.isPursuing : typeof legacyEducation.isPursuing === 'boolean' ? legacyEducation.isPursuing : true, electives: asStringArray(firstDefined(existing.electives, legacyEducation.electives, data.electives, [])) }; EDUCATION_TIERS.forEach((tier) => { academic.history[tier] = normaliseEducationTier(history[tier], legacyTiers[tier]); }); return academic; }
function normalisePersonal(data = {}) { const personal = data.personal || {}; const preferences = personal.preferences || {}; return { interests: asStringArray(firstDefined(personal.interests, data.interests, [])), hobbies: asStringArray(firstDefined(personal.hobbies, data.hobbies, [])), preferences: { tvShows: asStringArray(firstDefined(preferences.tvShows, data.tvShows, [])), movies: asStringArray(firstDefined(preferences.movies, data.movies, [])), games: asStringArray(firstDefined(preferences.games, data.games, [])), sports: asStringArray(firstDefined(preferences.sports, data.sports, [])) } }; }
export function normalizeStudentRecord(data = {}, id = null) { data = data.studentProfile && typeof data.studentProfile === 'object' ? { ...data, ...data.studentProfile } : data; const guardians = normaliseGuardian(data); const assignments = normaliseAssignments(data); const services = normaliseServices(data); const academic = normaliseAcademic(data); const personal = normalisePersonal(data); const dob = firstDefined(data.identity?.dateOfBirth, data.dob, data.dateOfBirth, null); return { schemaVersion: 2, id, identity: { fullName: firstDefined(data.identity?.fullName, data.name, data.fullName, ''), preferredName: firstDefined(data.identity?.preferredName, data.preferredName, ''), photoURL: firstDefined(data.identity?.photoURL, data.photoURL, data.profilePicture, ''), dateOfBirth: dob, gender: firstDefined(data.identity?.gender, data.gender, ''), pronouns: firstDefined(data.identity?.pronouns, data.pronouns, ''), country: firstDefined(data.identity?.country, data.country, 'India'), city: firstDefined(data.identity?.city, data.city, '') }, contact: { email: firstDefined(data.contact?.email, data.email, ''), mobile: { countryCode: firstDefined(data.contact?.mobile?.countryCode, data.contactCountryCode, null), number: firstDefined(data.contact?.mobile?.number, data.contactNumber, data.phone, '') }, city: firstDefined(data.contact?.city, data.city, '') }, family: { guardians }, institution: { id: firstDefined(data.institution?.id, data.institutionId, data.institutionID, ''), name: firstDefined(data.institution?.name, data.institutionName, data.schoolName, ''), academicYear: firstDefined(data.institution?.academicYear, data.academicYear, ''), enrollmentStatus: firstDefined(data.institution?.enrollmentStatus, data.enrollmentStatus, null) }, academic, personal, services, career: data.career || { status: services.career.status, interests: asStringArray(data.careerInterests || data.interests), aspirations: asStringArray(data.careerAspirations || data.careerGoals), profile: data.careerDNA || null, riasec: { code: firstDefined(data.riasecCode, data.careerDNA?.riasec?.code, ''), scores: firstDefined(data.riasecScores, data.careerDNA?.riasec?.scores, {}) }, roadmap: data.roadmap || null }, wellbeing: data.wellbeing || { status: services.wellbeing.status }, sen: data.sen || { status: services.sen.status }, assessments: asArray(data.assessments || data.assessmentHistory || data.careerAssessment).map((assessment) => assessment), goals: asArray(data.goals), relationships: { parents: guardians.map((guardian) => guardian.accountId).filter(Boolean), institutionId: firstDefined(data.academic?.current?.institutionId, data.academic?.institutionId, data.institution?.id, data.institutionId, data.institutionID, null), assignments }, onboarding: { profileComplete: data.profileComplete === true || data.onboarding?.profileComplete === true, completed: data.onboardingCompleted === true || data.onboarding?.completed === true }, governance: { consent: data.consent || data.consentStatus || null, createdAt: firstDefined(data.governance?.createdAt, data.createdAt, null), updatedAt: firstDefined(data.governance?.updatedAt, data.updatedAt, null) }, legacy: { primary_path: firstDefined(data.primary_path, null), studentTrack: firstDefined(data.studentTrack, null), assignedCounsellorId: firstDefined(data.assignedCounsellorId, null), parentUid: firstDefined(data.parentUid, null) } }; }
function currentInstitutionId(data = {}) { return firstDefined(data.academic?.current?.institutionId, data.academic?.institutionId, data.institution?.id, data.institutionId, data.institutionID, null); }
export const normaliseStudentRecord = normalizeStudentRecord;
export default normalizeStudentRecord;
