import React from 'react';
export default function InstitutionCareerReportStatus({student,onOpen}){const ready=student?.assessmentStatus==='completed'||student?.reportStatus==='ready';const claimed=Boolean(student?.claimedBy);return <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:12,flexWrap:'wrap'}}><div><div style={{fontSize:11,fontWeight:900,color:ready?'#15803d':'#64748b',textTransform:'uppercase'}}>{ready?'REPORT READY':claimed?'ASSESSMENT IN PROGRESS':'CODE NOT CLAIMED'}</div><div style={{fontSize:12,color:'#64748b',marginTop:3}}>{ready?'The institutional career report can be opened securely.':claimed?'The student must complete the assessment before a report can be opened.':'The student has not yet claimed the assessment code.'}</div></div>{ready&&<button type="button" onClick={onOpen} style={{border:0,borderRadius:10,padding:'10px 14px',background:'#4f46e5',color:'#fff',fontWeight:900,cursor:'pointer'}}>Open career report</button>}</div>}
