import React,{useMemo} from 'react';
import {STUDENT_PREMIUM_REPORT} from '../career/reportArchitecture';
import {buildInstitutionCareerReflection} from './careerReportDataContract';
const card={background:'#fff',border:'1px solid #e2e8f0',borderRadius:18,padding:20,boxShadow:'0 8px 28px rgba(15,23,42,.05)'};
export default function InstitutionCareerReportCoverage({report={}}){const contract=useMemo(()=>buildInstitutionCareerReflection(report),[report]);const byId=new Map(contract.map(row=>[row.id,row]));const rows=STUDENT_PREMIUM_REPORT.map(section=>{const row=byId.get(section.id);return {...section,status:row?.available?'Available':'Not available',tone:row?.available?'available':'missing'};});const available=rows.filter(x=>x.tone==='available').length;return <section style={{...card,marginTop:14}}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'baseline',flexWrap:'wrap'}}><div><div style={{fontSize:11,fontWeight:900,letterSpacing:1.2,color:'#4338ca'}}>REPORT COVERAGE</div><h2 style={{margin:'5px 0'}}>Student report → institutional reflection</h2><p style={{margin:0,color:'#64748b',fontSize:13}}>Coverage is based only on fields explicitly permitted by the Student → Admin contract. Assessment status is shown separately above.</p></div><div style={{fontWeight:950,color:'#0f172a'}}>{available}/{rows.length} available</div></div><div style={{display:'grid',gridTemplateColumns:'repeat(2,1fr)',gap:8,marginTop:14}}>{rows.map((r,i)=><div key={r.id} style={{padding:12,border:'1px solid #eef2f7',borderRadius:12,background:r.tone==='available'?'#f0fdf4':'#f8fafc'}}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><span style={{fontWeight:900,fontSize:13}}>{i+1}. {r.title}</span><span style={{fontSize:11,fontWeight:900,color:r.tone==='available'?'#15803d':'#64748b'}}>{r.status}</span></div><div style={{marginTop:4,fontSize:11,color:'#64748b'}}>{r.purpose}</div></div>)}</div></section>;
}
