import React from 'react';
import InstitutionCareerEvidencePanel from './InstitutionCareerEvidencePanel';
import InstitutionCareerReportCoverage from './InstitutionCareerReportCoverage';

const card={background:'#fff',border:'1px solid #e2e8f0',borderRadius:18,padding:20,boxShadow:'0 8px 28px rgba(15,23,42,.05)'};
const muted={color:'#64748b'};
const hasObject=v=>v&&typeof v==='object'&&!Array.isArray(v)&&Object.keys(v).length>0;
function Block({title,value}){return <section style={card}><h3 style={{marginTop:0}}>{title}</h3>{hasObject(value)?<pre style={{whiteSpace:'pre-wrap',fontSize:12,color:'#475569',margin:0}}>{JSON.stringify(value,null,2)}</pre>:<p style={{margin:0,...muted}}>Not available in this assessment.</p>}</section>}
function List({title,value,empty='Not available in this assessment.'}){const items=Array.isArray(value)?value:[];return <section style={card}><h3 style={{marginTop:0}}>{title}</h3>{items.length?items.slice(0,12).map((x,i)=><div key={x.id||i} style={{padding:'10px 0',borderBottom:'1px solid #eef2f7'}}><strong>{x.name||x.title||x.action||`Item ${i+1}`}</strong>{x.category&&<span style={{fontSize:12,...muted}}> · {x.category}</span>}</div>):<p style={{margin:0,...muted}}>{empty}</p>}</section>}
export default function InstitutionCareerReportShell({payload,onBack}){const report=payload?.report||{};const student=payload?.student||{};const intake=report.intake||report.developmentalContext||report.academicContext||{};const scores=report.scores||{};const careers=report.careerExploration||report.careerDirections||[];return <div style={{maxWidth:1100,margin:'0 auto',padding:'24px 18px 60px'}}><button type="button" onClick={onBack} style={{border:'1px solid #cbd5e1',background:'#fff',borderRadius:10,padding:'10px 14px',fontWeight:800,cursor:'pointer'}}>← Back to students</button><section style={{...card,marginTop:14,background:'linear-gradient(135deg,#0f172a,#1e293b)',color:'#fff'}}><div style={{color:'#fbbf24',fontSize:11,fontWeight:900,letterSpacing:1.5}}>VIDYAVANTAGE · INSTITUTIONAL CAREER REPORT</div><h1 style={{margin:'8px 0 5px'}}>{student.fullName||'Student Career Report'}</h1><p style={{margin:0,color:'#cbd5e1'}}>{student.className||'Class not supplied'} {student.section||''} · {student.rollNumber||'Roll no. not supplied'}</p></section><InstitutionCareerEvidencePanel assessmentEvidence={payload?.assessmentEvidence}/><InstitutionCareerReportCoverage report={report}/><div style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:12,marginTop:12}}>{[['RIASEC',scores.riasecCode||'—'],['Reasoning',scores.reasoning?.percent!=null?`${scores.reasoning.percent}%`:'—'],['Readiness',scores.readinessPercent!=null?`${scores.readinessPercent}%`:'—'],['Adaptability',scores.adaptabilityPercent!=null?`${scores.adaptabilityPercent}%`:'—']].map(([l,v])=><div key={l} style={card}><div style={{fontSize:11,fontWeight:900,...muted,textTransform:'uppercase'}}>{l}</div><div style={{fontSize:27,fontWeight:950,marginTop:5}}>{v}</div></div>)}</div><section style={{...card,marginTop:12}}><h3 style={{marginTop:0}}>Academic & developmental context</h3><div style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:10}}>{[['Education stage',intake.educationStage||intake.stage],['Class / year',intake.className||intake.year||student.className],['Stream',intake.stream],['Academic average',intake.academicAverage??intake.average]].map(([l,v])=><div key={l} style={{background:'#f8fafc',padding:12,borderRadius:10}}><div style={{fontSize:11,...muted}}>{l}</div><div style={{fontWeight:900,marginTop:4}}>{v??'Not available'}</div></div>)}</div>{Array.isArray(intake.subjects)&&intake.subjects.length>0&&<div style={{marginTop:10,fontSize:13}}><strong>Subjects:</strong> {intake.subjects.join(', ')}</div>}</section><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginTop:12}}><List title="Career directions" value={careers} empty="Career exploration data is not available."/><Block title="Career values" value={scores.values}/></div><Block title="Personality tendencies" value={scores.big5}/><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginTop:12}}><Block title="Preferred work environment" value={report.workEnvironment||report.preferredWorkEnvironment}/><Block title="Non-linear pathway analysis" value={report.pathwayAnalysis||report.pathways}/></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginTop:12}}><Block title="Stream & subject scenarios" value={report.streamAnalysis||report.streamScenarios}/><Block title="Education roadmap" value={report.educationRoadmap||report.education}/></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginTop:12}}><Block title="Skills & evidence plan" value={report.skillsEvidence||report.skillsPlan}/><Block title="Affordability, scholarships & friction" value={report.affordability||report.friction}/></div><List title="Alternative & unexpected careers" value={report.alternativeCareers||report.alternatives}/><List title="90-day career action roadmap" value={report.actionRoadmap||report.actionPlan||report.reflection?.recommendedActions}/><Block title="Counsellor conversation & limitations" value={report.counsellorReview||report.reviewLimitations}/><section style={{...card,marginTop:12,background:'#fffbeb',borderColor:'#fde68a'}}><h3 style={{marginTop:0}}>Professional-use boundary</h3><p style={{margin:0,color:'#713f12',lineHeight:1.7}}>This is an institutional career-exploration aid. It is not a clinical diagnosis, IQ score, hiring decision, disciplinary tool or guarantee of outcomes. Missing assessment evidence must not be interpreted as a negative result.</p></section></div>}
